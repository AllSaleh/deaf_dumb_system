import 'package:flutter/material.dart';

import '../../core/AppRequired/Text.dart';
class Downloads extends StatelessWidget {
  const Downloads({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: text(title: 'down'),
    );
  }
}